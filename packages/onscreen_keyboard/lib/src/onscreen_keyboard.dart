import 'package:flutter/widgets.dart';

class OnscreenKeyboard extends StatelessWidget {
  const OnscreenKeyboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('OnscreenKeyboard');
  }
}
